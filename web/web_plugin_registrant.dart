// Minimal web plugin registrant to avoid missing generated import for removed plugins.
// This file intentionally provides an empty plugin registration when the generated
// registrant includes plugins that are no longer present (e.g., record_web).

// ignore_for_file: public_member_api_docs

void registerPlugins([dynamic registrar]) {
  // No web plugins to register.
}
